# Binaries and build output
/zing
/bin/
/dist/
*.exe
*.dll
*.so
*.dylib

# Test and coverage output
*.test
*.out
coverage.out
coverage.html

# Go workspace file
go.work
go.work.sum

# Dependency directory (only if you vendor)
/vendor/

# Local environment and secrets
.env
.env.*
!.env.example

# Orchestrator worktree state and a safety net for zing.toml (design section
# 14): it holds github_token, and the real file lives at ~/.zing/, but this
# keeps a copy checked out into the repo from ever landing in a commit.
.zing/
zing.toml

# Editor and OS noise
.idea/
.vscode/
.DS_Store
*.swp
*.swo
*~
.#*

# lefthook local overrides
lefthook-local.yml
